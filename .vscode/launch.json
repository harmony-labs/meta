{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug meta",
            "cargo": {
                "args": [
                    "build",
                    "--package=meta"
                ]
            },
            "args": [
                "git status"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}